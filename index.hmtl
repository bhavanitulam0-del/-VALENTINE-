<!DOCTYPE html>
<html>
<head>
  <title>Valentine Question 💘</title>
  <style>
    body {
      text-align: center;
      font-family: Arial, sans-serif;
      background-color: #ffe6f2;
      padding-top: 100px;
    }
    h1 {
      color: #ff3366;
    }
    button {
      font-size: 20px;
      padding: 15px 30px;
      margin: 20px;
      border: none;
      border-radius: 10px;
      cursor: pointer;
    }
    #yes {
      background-color: #ff3366;
      color: white;
    }
    #no {
      background-color: #cccccc;
      position: absolute;
    }
  </style>
</head>
<body>

  <h1>Will you be my Valentine forever? Mr. Singham Rahul 💖</h1>

  <button id="yes" onclick="yesClicked()">YES 💍</button>
  <button id="no" onmouseover="moveNo()">NO 🙃</button>

  <script>
    function moveNo() {
      var x = Math.random() * (window.innerWidth - 100);
      var y = Math.random() * (window.innerHeight - 50);
      document.getElementById("no").style.left = x + "px";
      document.getElementById("no").style.top = y + "px";
    }

    function yesClicked() {
      document.body.innerHTML = "<h1>YAY! 💖 Valentine forever! 🥰💍</h1>";
    }
  </script>

</body>
</html>
